

const EditComponent = () => {

    //get id from url
    //get event from id
    //populate form with event data
    //on submit, update event with new data
    

    return (
        <div>
        <h1>EditComponent</h1>
        </div>
    );
};

export default EditComponent;